[package]
name = "trim-trailing"
version = "0.1.0"
edition = "2021"
description = "示例插件：保存前去除每行行尾空白（spec 附录 B）"

[lib]
crate-type = ["cdylib"]

[dependencies]
jasper-plugin-sdk = { path = "../../plugin-sdk" }

[profile.release]
opt-level = "s"
lto = true
